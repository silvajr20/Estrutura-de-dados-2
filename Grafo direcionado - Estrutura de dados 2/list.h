/*Essa função cria uma lista (Mapa), e retornará o endereço dela.
    Em caso de algum erro e não estiver vazia, retornará NULL. */
void * newMap ();

/* Essa função ir remover o mapa desejado. */
void removeMapa(void *c);

int newCity (char *a[], int p, int x, int y, void *l);